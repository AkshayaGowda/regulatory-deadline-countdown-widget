spring:
  application:
    name: tool-87-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/tool87
    username: postgres
    password: postgres123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms

  mail:   # ✅ CORRECT POSITION
    host: smtp.gmail.com
    port: 587
    username: akshayagowda.44@gmail.com
    password: oukfgoausbltntvu
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs

app:   # ✅ ONLY ONE APP BLOCK
  jwt:
    secret: change-me
    access-token-expiration-ms: 900000
    refresh-token-expiration-ms: 604800000

  notifications:
    from-email: akshayagowda.44@gmail.com
    daily-cron: 0 0 9 * * *
    time-zone: Asia/Calcutta
    deadline-alert-days: 1

  cors:
    allowed-origins: http://localhost,http://localhost:5173

  frontend-url: http://localhost:5173   # ⚠️ IMPORTANT

logging:
  level:
    org.springframework.security: INFO
    org.hibernate.SQL: INFO

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true